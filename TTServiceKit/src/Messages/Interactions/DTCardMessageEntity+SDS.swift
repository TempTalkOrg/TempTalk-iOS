//
//  Copyright (c) 2022 Open Whisper Systems. All rights reserved.
//

import Foundation
import GRDB
import SignalCoreKit

// NOTE: This file is generated by /Scripts/sds_codegen/sds_generate.py.
// Do not manually edit it, instead run `sds_codegen.sh`. Please contact Kris for support.

// MARK: - Record

public struct DTCardMessageEntityRecord: SDSRecord {
    public weak var delegate: SDSRecordDelegate?

    public var tableMetadata: SDSTableMetadata {
        DTCardMessageEntitySerializer.table
    }

    public static var databaseTableName: String {
        DTCardMessageEntitySerializer.table.tableName
    }

    public var id: Int64?

    // This defines all of the columns used in the table
    // where this model (and any subclasses) are persisted.
    public let recordType: SDSRecordType
    public let uniqueId: String

    // Properties
    public let appId: String
    public let cardId: String
    public let content: String
    public let contentType: UInt32
    public let creator: String?
    public let fixedWidth: Bool
    public let timestamp: UInt64
    public let version: UInt32

    public enum CodingKeys: String, CodingKey, ColumnExpression, CaseIterable {
        case id
        case recordType
        case uniqueId
        case appId
        case cardId
        case content
        case contentType
        case creator
        case fixedWidth
        case timestamp
        case version
    }

    public static func columnName(_ column: DTCardMessageEntityRecord.CodingKeys, fullyQualified: Bool = false) -> String {
        fullyQualified ? "\(databaseTableName).\(column.rawValue)" : column.rawValue
    }

    public func didInsert(with rowID: Int64, for column: String?) {
        guard let delegate = delegate else {
            owsFailDebug("Missing delegate.")
            return
        }
        delegate.updateRowId(rowID)
    }
}

// MARK: - Row Initializer

public extension DTCardMessageEntityRecord {
    static var databaseSelection: [SQLSelectable] {
        CodingKeys.allCases
    }

    init(row: Row) {
        id = row[0]
        recordType = row[1]
        uniqueId = row[2]
        appId = row[3]
        cardId = row[4]
        content = row[5]
        contentType = row[6]
        creator = row[7]
        fixedWidth = row[8]
        timestamp = row[9]
        version = row[10]
    }
}

// MARK: - StringInterpolation

public extension String.StringInterpolation {
    mutating func appendInterpolation(dTCardMessageEntityColumn column: DTCardMessageEntityRecord.CodingKeys) {
        appendLiteral(DTCardMessageEntityRecord.columnName(column))
    }
    mutating func appendInterpolation(dTCardMessageEntityColumnFullyQualified column: DTCardMessageEntityRecord.CodingKeys) {
        appendLiteral(DTCardMessageEntityRecord.columnName(column, fullyQualified: true))
    }
}

// MARK: - Deserialization

// TODO: Rework metadata to not include, for example, columns, column indices.
extension DTCardMessageEntity {
    // This method defines how to deserialize a model, given a
    // database row.  The recordType column is used to determine
    // the corresponding model class.
    class func fromRecord(_ record: DTCardMessageEntityRecord) throws -> DTCardMessageEntity {

        guard let recordId = record.id else {
            throw SDSError.invalidValue
        }

        switch record.recordType {
        case .dTCardMessageEntity:

            let uniqueId: String = record.uniqueId
            let appId: String = record.appId
            let cardId: String = record.cardId
            let content: String = record.content
            let contentType: UInt32 = record.contentType
            let creator: String? = record.creator
            let fixedWidth: Bool = record.fixedWidth
            let timestamp: UInt64 = record.timestamp
            let version: UInt32 = record.version

            return DTCardMessageEntity(grdbId: recordId,
                                       uniqueId: uniqueId,
                                       appId: appId,
                                       cardId: cardId,
                                       content: content,
                                       contentType: contentType,
                                       creator: creator,
                                       fixedWidth: fixedWidth,
                                       timestamp: timestamp,
                                       version: version)

        default:
            owsFailDebug("Unexpected record type: \(record.recordType)")
            throw SDSError.invalidValue
        }
    }
}

// MARK: - SDSModel

extension DTCardMessageEntity: SDSModel {
    public var serializer: SDSSerializer {
        // Any subclass can be cast to it's superclass,
        // so the order of this switch statement matters.
        // We need to do a "depth first" search by type.
        switch self {
        default:
            return DTCardMessageEntitySerializer(model: self)
        }
    }

    public func asRecord() throws -> SDSRecord {
        try serializer.asRecord()
    }

    public var sdsTableName: String {
        DTCardMessageEntityRecord.databaseTableName
    }

    public static var table: SDSTableMetadata {
        DTCardMessageEntitySerializer.table
    }
}

// MARK: - DeepCopyable

extension DTCardMessageEntity: DeepCopyable {

    public func deepCopy() throws -> AnyObject {
        // Any subclass can be cast to it's superclass,
        // so the order of this switch statement matters.
        // We need to do a "depth first" search by type.
        guard let id = self.grdbId?.int64Value else {
            throw OWSAssertionError("Model missing grdbId.")
        }

        do {
            let modelToCopy = self
            assert(type(of: modelToCopy) == DTCardMessageEntity.self)
            let uniqueId: String = modelToCopy.uniqueId
            let appId: String = modelToCopy.appId
            let cardId: String = modelToCopy.cardId
            let content: String = modelToCopy.content
            let contentType: UInt32 = modelToCopy.contentType
            let creator: String? = modelToCopy.creator
            let fixedWidth: Bool = modelToCopy.fixedWidth
            let timestamp: UInt64 = modelToCopy.timestamp
            let version: UInt32 = modelToCopy.version

            return DTCardMessageEntity(grdbId: id,
                                       uniqueId: uniqueId,
                                       appId: appId,
                                       cardId: cardId,
                                       content: content,
                                       contentType: contentType,
                                       creator: creator,
                                       fixedWidth: fixedWidth,
                                       timestamp: timestamp,
                                       version: version)
        }

    }
}

// MARK: - Table Metadata

extension DTCardMessageEntitySerializer {

    // This defines all of the columns used in the table
    // where this model (and any subclasses) are persisted.
    static var idColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "id", columnType: .primaryKey) }
    static var recordTypeColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "recordType", columnType: .int64) }
    static var uniqueIdColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "uniqueId", columnType: .unicodeString, isUnique: true) }
    // Properties
    static var appIdColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "appId", columnType: .unicodeString) }
    static var cardIdColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "cardId", columnType: .unicodeString) }
    static var contentColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "content", columnType: .unicodeString) }
    static var contentTypeColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "contentType", columnType: .int64) }
    static var creatorColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "creator", columnType: .unicodeString, isOptional: true) }
    static var fixedWidthColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "fixedWidth", columnType: .int) }
    static var timestampColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "timestamp", columnType: .int64) }
    static var versionColumn: SDSColumnMetadata { SDSColumnMetadata(columnName: "version", columnType: .int64) }

    // TODO: We should decide on a naming convention for
    //       tables that store models.
    public static var table: SDSTableMetadata {
        SDSTableMetadata(collection: DTCardMessageEntity.collection(),
                         tableName: "model_DTCardMessageEntity",
                         columns: [
        idColumn,
        recordTypeColumn,
        uniqueIdColumn,
        appIdColumn,
        cardIdColumn,
        contentColumn,
        contentTypeColumn,
        creatorColumn,
        fixedWidthColumn,
        timestampColumn,
        versionColumn
        ])
    }
}

// MARK: - Save/Remove/Update

@objc
public extension DTCardMessageEntity {
    func anyInsert(transaction: SDSAnyWriteTransaction) {
        sdsSave(saveMode: .insert, transaction: transaction)
    }

    // Avoid this method whenever feasible.
    //
    // If the record has previously been saved, this method does an overwriting
    // update of the corresponding row, otherwise if it's a new record, this
    // method inserts a new row.
    //
    // For performance, when possible, you should explicitly specify whether
    // you are inserting or updating rather than calling this method.
    func anyUpsert(transaction: SDSAnyWriteTransaction) {
        let isInserting: Bool
        if DTCardMessageEntity.anyFetch(uniqueId: uniqueId, transaction: transaction) != nil {
            isInserting = false
        } else {
            isInserting = true
        }
        sdsSave(saveMode: isInserting ? .insert : .update, transaction: transaction)
    }

    // This method is used by "updateWith..." methods.
    //
    // This model may be updated from many threads. We don't want to save
    // our local copy (this instance) since it may be out of date.  We also
    // want to avoid re-saving a model that has been deleted.  Therefore, we
    // use "updateWith..." methods to:
    //
    // a) Update a property of this instance.
    // b) If a copy of this model exists in the database, load an up-to-date copy,
    //    and update and save that copy.
    // b) If a copy of this model _DOES NOT_ exist in the database, do _NOT_ save
    //    this local instance.
    //
    // After "updateWith...":
    //
    // a) Any copy of this model in the database will have been updated.
    // b) The local property on this instance will always have been updated.
    // c) Other properties on this instance may be out of date.
    //
    // All mutable properties of this class have been made read-only to
    // prevent accidentally modifying them directly.
    //
    // This isn't a perfect arrangement, but in practice this will prevent
    // data loss and will resolve all known issues.
    func anyUpdate(transaction: SDSAnyWriteTransaction, block: (DTCardMessageEntity) -> Void) {

        block(self)

        guard let dbCopy = type(of: self).anyFetch(uniqueId: uniqueId,
                                                   transaction: transaction) else {
            return
        }

        // Don't apply the block twice to the same instance.
        // It's at least unnecessary and actually wrong for some blocks.
        // e.g. `block: { $0 in $0.someField++ }`
        if dbCopy !== self {
            block(dbCopy)
        }

        dbCopy.sdsSave(saveMode: .update, transaction: transaction)
    }

    // This method is an alternative to `anyUpdate(transaction:block:)` methods.
    //
    // We should generally use `anyUpdate` to ensure we're not unintentionally
    // clobbering other columns in the database when another concurrent update
    // has occurred.
    //
    // There are cases when this doesn't make sense, e.g. when  we know we've
    // just loaded the model in the same transaction. In those cases it is
    // safe and faster to do a "overwriting" update
    func anyOverwritingUpdate(transaction: SDSAnyWriteTransaction) {
        sdsSave(saveMode: .update, transaction: transaction)
    }

    func anyRemove(transaction: SDSAnyWriteTransaction) {
        sdsRemove(transaction: transaction)
    }

    func anyReload(transaction: SDSAnyReadTransaction) {
        anyReload(transaction: transaction, ignoreMissing: false)
    }

    func anyReload(transaction: SDSAnyReadTransaction, ignoreMissing: Bool) {
        guard let latestVersion = type(of: self).anyFetch(uniqueId: uniqueId, transaction: transaction) else {
            if !ignoreMissing {
                owsFailDebug("`latest` was unexpectedly nil")
            }
            return
        }

        setValuesForKeys(latestVersion.dictionaryValue)
    }
}

// MARK: - DTCardMessageEntityCursor

@objc
public class DTCardMessageEntityCursor: NSObject, SDSCursor {
    private let transaction: GRDBReadTransaction
    private let cursor: RecordCursor<DTCardMessageEntityRecord>?

    init(transaction: GRDBReadTransaction, cursor: RecordCursor<DTCardMessageEntityRecord>?) {
        self.transaction = transaction
        self.cursor = cursor
    }

    public func next() throws -> DTCardMessageEntity? {
        guard let cursor = cursor else {
            return nil
        }
        guard let record = try cursor.next() else {
            return nil
        }
        return try DTCardMessageEntity.fromRecord(record)
    }

    public func all() throws -> [DTCardMessageEntity] {
        var result = [DTCardMessageEntity]()
        while true {
            guard let model = try next() else {
                break
            }
            result.append(model)
        }
        return result
    }
}

// MARK: - Obj-C Fetch

// TODO: We may eventually want to define some combination of:
//
// * fetchCursor, fetchOne, fetchAll, etc. (ala GRDB)
// * Optional "where clause" parameters for filtering.
// * Async flavors with completions.
//
// TODO: I've defined flavors that take a read transaction.
//       Or we might take a "connection" if we end up having that class.
@objc
public extension DTCardMessageEntity {
    class func grdbFetchCursor(transaction: GRDBReadTransaction) -> DTCardMessageEntityCursor {
        let database = transaction.database
        do {
            let cursor = try DTCardMessageEntityRecord.fetchCursor(database)
            return DTCardMessageEntityCursor(transaction: transaction, cursor: cursor)
        } catch {
            owsFailDebug("Read failed: \(error)")
            return DTCardMessageEntityCursor(transaction: transaction, cursor: nil)
        }
    }

    // Fetches a single model by "unique id".
    class func anyFetch(uniqueId: String,
                        transaction: SDSAnyReadTransaction) -> DTCardMessageEntity? {
        assert(uniqueId.count > 0)

        switch transaction.readTransaction {
        //Kris GRDB
        case .grdbRead(let grdbTransaction):
            let sql = "SELECT * FROM \(DTCardMessageEntityRecord.databaseTableName) WHERE \(dTCardMessageEntityColumn: .uniqueId) = ?"
            return grdbFetchOne(sql: sql, arguments: [uniqueId], transaction: grdbTransaction)
        }
    }

    // Traverses all records.
    // Records are not visited in any particular order.
    class func anyEnumerate(transaction: SDSAnyReadTransaction,
                            block: @escaping (DTCardMessageEntity, UnsafeMutablePointer<ObjCBool>) -> Void) {
        anyEnumerate(transaction: transaction, batched: false, block: block)
    }

    // Traverses all records.
    // Records are not visited in any particular order.
    class func anyEnumerate(transaction: SDSAnyReadTransaction,
                            batched: Bool = false,
                            block: @escaping (DTCardMessageEntity, UnsafeMutablePointer<ObjCBool>) -> Void) {
        let batchSize = batched ? Batching.kDefaultBatchSize : 0
        anyEnumerate(transaction: transaction, batchSize: batchSize, block: block)
    }

    // Traverses all records.
    // Records are not visited in any particular order.
    //
    // If batchSize > 0, the enumeration is performed in autoreleased batches.
    class func anyEnumerate(transaction: SDSAnyReadTransaction,
                            batchSize: UInt,
                            block: @escaping (DTCardMessageEntity, UnsafeMutablePointer<ObjCBool>) -> Void) {
        switch transaction.readTransaction {
        //Kris GRDB
        case .grdbRead(let grdbTransaction):
            let cursor = DTCardMessageEntity.grdbFetchCursor(transaction: grdbTransaction)
            Batching.loop(batchSize: batchSize,
                          loopBlock: { stop in
                                do {
                                    guard let value = try cursor.next() else {
                                        stop.pointee = true
                                        return
                                    }
                                    block(value, stop)
                                } catch let error {
                                    owsFailDebug("Couldn't fetch model: \(error)")
                                }
                              })
        }
    }

    // Traverses all records' unique ids.
    // Records are not visited in any particular order.
    class func anyEnumerateUniqueIds(transaction: SDSAnyReadTransaction,
                                     block: @escaping (String, UnsafeMutablePointer<ObjCBool>) -> Void) {
        anyEnumerateUniqueIds(transaction: transaction, batched: false, block: block)
    }

    // Traverses all records' unique ids.
    // Records are not visited in any particular order.
    class func anyEnumerateUniqueIds(transaction: SDSAnyReadTransaction,
                                     batched: Bool = false,
                                     block: @escaping (String, UnsafeMutablePointer<ObjCBool>) -> Void) {
        let batchSize = batched ? Batching.kDefaultBatchSize : 0
        anyEnumerateUniqueIds(transaction: transaction, batchSize: batchSize, block: block)
    }

    // Traverses all records' unique ids.
    // Records are not visited in any particular order.
    //
    // If batchSize > 0, the enumeration is performed in autoreleased batches.
    class func anyEnumerateUniqueIds(transaction: SDSAnyReadTransaction,
                                     batchSize: UInt,
                                     block: @escaping (String, UnsafeMutablePointer<ObjCBool>) -> Void) {
        switch transaction.readTransaction {
        //Kris GRDB
        case .grdbRead(let grdbTransaction):
            grdbEnumerateUniqueIds(transaction: grdbTransaction,
                                   sql: """
                    SELECT \(dTCardMessageEntityColumn: .uniqueId)
                    FROM \(DTCardMessageEntityRecord.databaseTableName)
                """,
                batchSize: batchSize,
                block: block)
        }
    }

    // Does not order the results.
    class func anyFetchAll(transaction: SDSAnyReadTransaction) -> [DTCardMessageEntity] {
        var result = [DTCardMessageEntity]()
        anyEnumerate(transaction: transaction) { (model, _) in
            result.append(model)
        }
        return result
    }

    // Does not order the results.
    class func anyAllUniqueIds(transaction: SDSAnyReadTransaction) -> [String] {
        var result = [String]()
        anyEnumerateUniqueIds(transaction: transaction) { (uniqueId, _) in
            result.append(uniqueId)
        }
        return result
    }

    class func anyCount(transaction: SDSAnyReadTransaction) -> UInt {
        switch transaction.readTransaction {
        //Kris GRDB
        case .grdbRead(let grdbTransaction):
            return DTCardMessageEntityRecord.ows_fetchCount(grdbTransaction.database)
        }
    }

    // WARNING: Do not use this method for any models which do cleanup
    //          in their anyWillRemove(), anyDidRemove() methods.
    class func anyRemoveAllWithoutInstantation(transaction: SDSAnyWriteTransaction) {
        switch transaction.writeTransaction {
        //Kris GRDB
        case .grdbWrite(let grdbTransaction):
            do {
                try DTCardMessageEntityRecord.deleteAll(grdbTransaction.database)
            } catch {
                owsFailDebug("deleteAll() failed: \(error)")
            }
        }

        if ftsIndexMode != .never {
            FullTextSearchFinder.allModelsWereRemoved(collection: collection(), transaction: transaction)
        }
    }

    class func anyRemoveAllWithInstantation(transaction: SDSAnyWriteTransaction) {
        // To avoid mutationDuringEnumerationException, we need
        // to remove the instances outside the enumeration.
        let uniqueIds = anyAllUniqueIds(transaction: transaction)

        var index: Int = 0
        Batching.loop(batchSize: Batching.kDefaultBatchSize,
                      loopBlock: { stop in
            guard index < uniqueIds.count else {
                stop.pointee = true
                return
            }
            let uniqueId = uniqueIds[index]
            index += 1
            guard let instance = anyFetch(uniqueId: uniqueId, transaction: transaction) else {
                owsFailDebug("Missing instance.")
                return
            }
            instance.anyRemove(transaction: transaction)
        })

        if ftsIndexMode != .never {
            FullTextSearchFinder.allModelsWereRemoved(collection: collection(), transaction: transaction)
        }
    }

    class func anyExists(
        uniqueId: String,
        transaction: SDSAnyReadTransaction
    ) -> Bool {
        assert(uniqueId.count > 0)

        switch transaction.readTransaction {
        //Kris GRDB
        case .grdbRead(let grdbTransaction):
            let sql = "SELECT EXISTS ( SELECT 1 FROM \(DTCardMessageEntityRecord.databaseTableName) WHERE \(dTCardMessageEntityColumn: .uniqueId) = ? )"
            let arguments: StatementArguments = [uniqueId]
            return try! Bool.fetchOne(grdbTransaction.database, sql: sql, arguments: arguments) ?? false
        }
    }
}

// MARK: - Swift Fetch

public extension DTCardMessageEntity {
    class func grdbFetchCursor(sql: String,
                               arguments: StatementArguments = StatementArguments(),
                               transaction: GRDBReadTransaction) -> DTCardMessageEntityCursor {
        do {
            let sqlRequest = SQLRequest<Void>(sql: sql, arguments: arguments, cached: true)
            let cursor = try DTCardMessageEntityRecord.fetchCursor(transaction.database, sqlRequest)
            return DTCardMessageEntityCursor(transaction: transaction, cursor: cursor)
        } catch {
            Logger.verbose("sql: \(sql)")
            owsFailDebug("Read failed: \(error)")
            return DTCardMessageEntityCursor(transaction: transaction, cursor: nil)
        }
    }

    class func grdbFetchOne(sql: String,
                            arguments: StatementArguments = StatementArguments(),
                            transaction: GRDBReadTransaction) -> DTCardMessageEntity? {
        assert(sql.count > 0)

        do {
            let sqlRequest = SQLRequest<Void>(sql: sql, arguments: arguments, cached: true)
            guard let record = try DTCardMessageEntityRecord.fetchOne(transaction.database, sqlRequest) else {
                return nil
            }

            return try DTCardMessageEntity.fromRecord(record)
        } catch {
            owsFailDebug("error: \(error)")
            return nil
        }
    }
}

// MARK: - SDSSerializer

// The SDSSerializer protocol specifies how to insert and update the
// row that corresponds to this model.
class DTCardMessageEntitySerializer: SDSSerializer {

    private let model: DTCardMessageEntity
    public required init(model: DTCardMessageEntity) {
        self.model = model
    }

    // MARK: - Record

    func asRecord() throws -> SDSRecord {
        let id: Int64? = model.grdbId?.int64Value

        let recordType: SDSRecordType = .dTCardMessageEntity
        let uniqueId: String = model.uniqueId

        // Properties
        let appId: String = model.appId
        let cardId: String = model.cardId
        let content: String = model.content
        let contentType: UInt32 = model.contentType
        let creator: String? = model.creator
        let fixedWidth: Bool = model.fixedWidth
        let timestamp: UInt64 = model.timestamp
        let version: UInt32 = model.version

        return DTCardMessageEntityRecord(delegate: model, id: id, recordType: recordType, uniqueId: uniqueId, appId: appId, cardId: cardId, content: content, contentType: contentType, creator: creator, fixedWidth: fixedWidth, timestamp: timestamp, version: version)
    }
}

// MARK: - Deep Copy

#if TESTABLE_BUILD
@objc
public extension DTCardMessageEntity {
    // We're not using this method at the moment,
    // but we might use it for validation of
    // other deep copy methods.
    func deepCopyUsingRecord() throws -> DTCardMessageEntity {
        guard let record = try asRecord() as? DTCardMessageEntityRecord else {
            throw OWSAssertionError("Could not convert to record.")
        }
        return try DTCardMessageEntity.fromRecord(record)
    }
}
#endif
                    