# Fastlane Environment Variables Template
#
# Copy this file to .env and fill in your actual values
# DO NOT commit the actual .env file to version control
#
# Usage: 
# 1. cp .env.example .env
# 2. Edit .env with your actual values
# 3. Add .env to .gitignore

# Xcode Configuration
SCHEME=YourAppScheme
CONFIGURATION=Release
PROJECT=YourApp.xcodeproj
INFO_PLIST_PATH=YourApp/Info.plist

# Firebase Configuration (if using Firebase)
GOOGLE_INFO_PLIST_PATH=YourApp/GoogleService-Info.plist

# Apple Developer Account
APPLE_USERNAME=your.email@example.com
TEAM_ID=YOUR_TEAM_ID
ITC_TEAM_ID=YOUR_ITC_TEAM_ID

# App Identifiers
APP_IDENTIFIER=com.yourcompany.yourapp
SHARE_EXTENSION_IDENTIFIER=com.yourcompany.yourapp.share
NSE_IDENTIFIER=com.yourcompany.yourapp.NSE

# Code Signing (if using match)
MATCH_GIT_URL=https://github.com/yourorg/ios-certificates.git
MATCH_APP_IDENTIFIER=com.yourcompany.yourapp
MATCH_TYPE=appstore

# Optional: Crash Reporting
CRASHLYTICS_API_TOKEN=your_crashlytics_token_here

# Optional: Teams notifications